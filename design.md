# Design — 流れ／星霜

「**和紙と金箔**」— このアプリの意匠を固定した文書。ページを改修するときは
まずこれを読む。ページごとに作り直さず、体系を育てるときはこの文書を先に直す。

参照系統は**素材感・工芸寄りのラグジュアリー**（中川政七商店・山井本家・Hermès のクラフト面）。
禁欲的な余白ではなく、**地合いがある**ことが贅沢という立場を取る。

> **正本は `app/globals.css`。この文書は方針であって値の写しではない。**
> 実値が食い違ったらコードが正しい。Figma ミラーも同じ扱い（[docs/SPEC.md](docs/SPEC.md) §7）。

---

## Genre

editorial。暗い地は実時刻という**事実**であってムードではない（`html[data-sky]` が
太陽高度で決まる）。声は editorial に統一する。

## Macrostructure

- **アプリ画面**（5タブ・`/`）… **Long Document** — 縦に読み下す一本の流れ。
  既存の `FlowLine`（一本の川が降りる）と同じ隠喩。**章の仕切りは余白、
  項の仕切りは罫**（PR #50。以前は「仕切りは罫ではなく余白」としていたが、
  項まで余白で仕切ると箱が必要になり、箱の反復に戻ってしまう）。
- **入口体験**（`/welcome`）… 五幕の縦スクロール。Long Document の変種。

## この方向がこのアプリに合っている理由

語彙が既に工芸のものだった。昼の状態の名は「**生成りの光**」＝未晒しの布。
節気24色は若草・紅梅・萌黄・瑠璃・柿・藍鉄 —— すべて**染めの名前**であって
色の名前ではない。金は「聖域色」と呼ばれている。

足りなかったのは **地合い（テクスチャ）** と、**ガラスを紙に置き換えること**だった。

---

## 中心概念 — 角は紙、地は硝子

`box-shadow` による浮遊感は捨てたままにする。**角の立ちかたと縁の細さは紙**のもので、
**地は硝子**にする（PR-E）。

| | 旧 | 現在 |
|---|---|---|
| 角 | `16px` | `3px` — **紙は角が立つ**。硝子にしても丸めない |
| 影 | `--elev-1` | なし（外へ落ちる影は使わない。奥行きは内側の稜で出す） |
| ぼかし | `backdrop-filter` | クローム3層 ＋ ひとこと ＋ **本文の面**（`.card` `.lucky-action`） |
| 縁 | 副次的 | 硝子の面は `--glass-rim`、面を持たない段は `--hairline` |

> **角丸は硝子と一緒に動かさない。** PR-E では 3px を据え置いた。
> 二つ同時に変えると、どちらが効いたのか切り分けられなくなる。

---

## 硝子 — 素材の法

かつては「硝子は読み手と本文のあいだに割り込む層のもの、紙は本文そのもの」として
クローム3層＋ひとことの4面に限っていた。**PR-E で本文の面も硝子にした**ので、
法を書き換える。

> **硝子は地であり、紙は角と縁である。**
> **そして硝子の濃さは、意匠ではなく可読性が決める。**

後半が新しい部分で、こちらのほうが重い。半透明の面の上では、
**文字のコントラストが背後によって変わる**。このアプリの背後には金の流れ線と星があり、
そこが最悪ケースになる。不透明度は目分量で決めてはならない。

### 不透明度は測って決めた

`playwright/measure-contrast.mjs` が、空4状態 × 3タブの全本文について
**実際に描かれたピクセル**から実効コントラストを出す。
（同じ画面を2枚撮り、2枚目は文字だけ透明にする。差分＝グリフの位置、
その位置の地は2枚目から直接読める。）

| 地の濃さ | 新たに AA 4.5:1 を割った本文 |
|---|---|
| 62% → 52% | **16件**（`.nenun-card` `.meishiki` ほか） |
| 80% → 72% | 1件 |
| 84% → 77% | 0件（ただし最小 4.50 ちょうどで余裕なし） |
| **86% → 79%** | **0件・最小 4.64** ← 採用 |

**意匠より WCAG が優先する。**下げれば硝子は美しくなるが、
そのぶん本文が読めなくなる方向にしか動かない。

なお、こうして決まった 86%→79% は、硝子にする前の `--veil-card`（86% 一色）に
近い。**硝子にしたが、可読性の要求で紙寄りの濃さに落ち着いた**というのが正確な記述で、
効いているのはぼかし・彩度・縦のグラデ・稜・縁のほうである。

### 面を持たない段は硝子を脱ぐ

`.chip`（生まれのしるし）は「地＝罫だけ」の段なので、`.card` が着せる硝子を
**明示的に脱がせる**（`backdrop-filter: none` と `box-shadow: none`）。
`background: none` だけでは、面が無いのに背後だけぼける・縁の無いところに
光の線が出る、という辻褄の合わない姿になる。

**許可した面の一覧は `__tests__/design-tokens.test.ts` の `GLASS_SURFACES` が正。**
増やすときはその配列を編集する＝PR の差分に必ず現れる、という運用にする。
散文の「限定的に使う」は守れないが、配列は守れる。
（面が6つに増えたぶん「許可した面にしか無い」検査の締める力は落ちた。
ただし対の2本 —「許可した面はすべて実際に硝子」「不透明退避がある」— は価値が残る。）

### 硝子には三つ要る

ぼかしただけの半透明は硝子ではなく**霧**になる。

| | なぜ |
|---|---|
| **① `saturate()`** | 硝子越しでも彩度は落ちない。blur だけの面は灰へ寄る。**ここが質の最大の差** |
| **② 片側から当たる光** | 地は平らな塗りではなく縦のグラデ（`--veil-*` から `--bg-lo` へ落とす） |
| **③ 縁が拾う光の線** | 板の厚みが見える内側 1px。夜は星明かり、宵は茜、生成りの地では最明部（`--glass-edge` が空に追随して自動で切り替わる） |

トークンは `--glass-blur` `--glass-sat` `--glass-edge` `--glass-rim` `--glass-cast`。
**地の色は硝子側に持たない** — 面ごとの `--veil-*` を使う。`--veil-*` は `--bg-*` /
`--surface-*` 由来なので空4状態にそのまま追随し、硝子側に per-theme の写しが増えない。

### 硝子が可読性を削ってはならない

`backdrop-filter` 非対応のブラウザでは、半透明の地だけが残って本文がクロームの下を
素通しで走る。**硝子は装飾だが、可読性は装飾ではない。** `@supports not (…)` で
硝子面のすべてを不透明へ戻す（新しい硝子面を足したらここにも足す。テストが漏れを落とす）。

**`-webkit-backdrop-filter` は手で書かない。** ビルド（Lightning CSS）が前置する。
標準プロパティを先・`-webkit-` を後に書くと Lightning CSS は**標準側を落とす**ため、
unprefixed しか解さない Firefox でぼかしが消える（実測・PR #50 で修正）。

## 階層 — 硝子の濃さ・面の有無

すべてを同じ強さで見せない。段は **面の有無** と、面がある場合は **硝子の濃さ** で付ける。

| 段 | 素材 | 何 |
|---|---|---|
| **主** | **薄い硝子**（62%→52%） | フローメーター（面を持たず大きな数字だけ・余白が額縁）／ひとこと |
| **副** | **濃い硝子**（86%→79%） | 開運アクション・命式・暦・九星盤・事典 — 行動と図表の層 |
| **地** | **罫だけ**（硝子を脱ぐ） | 兆し・天体の便り・次の転機・生まれのしるし — 読む層 |

**主のほうが薄い**のが要点。透けるほど背後の空と流れ線を含むので、
薄い側が前に出る。強度そのものが階層を言う、という規約はぼかし
（本文の面は `--blur-sm` 10px・クロームは 18px）にも同じく効いている。

**主を大きくする方向で作る**（副や地を沈める方向ではない）。
`--text-faint` は明るい地で 4.0:1 しかなく、小さな字に使うと AA を割る。
静けさをコントラストで買わない。

### 箱を積むのをやめる

同じ角丸・同じ縁の面が5個も6個も続くのが、このアプリで最も「一般的な SaaS」に
見えていた場所だった。**これは面を取ることでしか直らない**（角丸を変えても、
影を消しても、反復そのものは残る）。

面を取ると、副次的に大きいことが二つ起きる。

1. **背後を流れ線が通る。** 線はこのアプリの署名なのに、カードで塞いでいた。
   意匠として本末転倒だった
2. **縁と地の分だけ幅が戻る。** 本文が列いっぱいに組める

仕切りは罫（`--hairline-soft`）一本。その左端に色の断片（44×2px）を**重ねて**
兆しの別を出す。罫の上に乗るので、色を持たない項も同じ位置に同じ長さの罫が通り、
律動が揃う。触れると 96px に伸びる。

結果、**今日タブのスクロール総量は 7.7% 減った**（字は大きくしたのに短くなった）。
箱の padding と縁が食っていた縦が、そのまま余白と字に回っている。

## 書体の対比

見出しは明朝、本文は Noto Sans。**同じ書体でウェイトだけ変えても差は出ない。**
書体が変わることそのものを階層にする。

| | 書体 | 大きさ |
|---|---|---|
| ワードマーク | 明朝 800 | `clamp(1.375rem, …, 1.9rem)` |
| 章題 | 明朝 700 | `--fs-headline`（1.25→1.75rem） |
| ひとこと | 明朝 500 | `--fs-title` ＋ `--lh-loose` — UIの一行ではなく**引用文**として組む |
| 記事見出し | 明朝 700 | `--fs-title` |
| 本文 | Noto Sans 400 | `--fs-body` |

章題が 1.02rem だった頃は本文（0.875rem）との比が **1.16 倍**しかなく、
見出しが見出しに見えないまま画面が「同じ強さの帯」の反復になっていた。
`--fs-headline` まで上げると比は **1.4〜2.0 倍**になり、ここで初めて構図が生まれる。

## 地合い

`feTurbulence` を **data-URI で一度だけラスタライズ**して背景画像にする。
ライブフィルタとして回さない（毎フレーム再計算になる）。

- `mix-blend-mode: overlay` で明暗**両方向**に振らせる。`multiply`（片方向）だと
  生成りの暖色が灰へ濁って「くすんだ紙」になる
- 濃さは地の明暗で変える：夜 `.55` / 昼 `.40`
- `position: fixed`（`.skyfield` と同じ）。絶対配置の子にすると内容と一緒に流れる
- `background-attachment: fixed` は使わない（モバイルで著しく重い）

## 節気の染め

`--sekki` をその日の地にごく薄く置く（夜 7%→3% / 昼 9%→4%）。
24日ごとに世界の色が静かに変わる。**このアプリだけが持てる贅沢**。

`--sekki` の**本文文字への使用は引き続き禁止**（[docs/SPEC.md](docs/SPEC.md) §7 の規約は維持）。
地・罫・眉・装飾に限る。

---

## Theme

**既存パレットを継承する。新規パレットは作らない。** 変えるのは
「どれだけ使うか、どこに置くか」であって色そのものではない。

- 4つの空（`night` / `dusk` / `day` / `dawn`）… 実時刻連動
- 二十四節気（`--sekki-l` / `--sekki-d`）… 24色
- 五行の今日の色（`--lucky` 系6変数）… 5色
- 金（`--gold-100`..`600`）… 聖域色

値は `app/globals.css` の `:root` および `[data-sky]` ブロックが正。**OKLCH へは変換しない**
（`__tests__/design-tokens.test.ts` が hex 前提で検査している）。

## Typography

- **Display** … Shippori Mincho **500**/700/800。ワードマーク・大きな漢字値・数字・章題・
  **記事見出し・ひとこと**（PR #50 で見出し全般へ広げた。詳細は「書体の対比」）
- **Body** … Noto Sans JP 400/500/600。本文とUI
- **文字サイズ下限 11px**。10px の例外は許可リスト運用（`.cal-` `.mk-` `.sf-retro small`）
- 眉ラベルは `--fs-caption` ＋ `--tr-widest`(.24em)。系統名はここに置く

## Spacing

4px 基数の `--sp-3xs`..`5xl`。**一目性を守るため開きすぎない**。

- セクション間 `--sp-xl`(24px)、**章題の前は `--sp-3xl`(40px)**
  （PR #50。章題を `--fs-headline` まで上げたので、`--sp-2xl` では頭が詰まる。
  この 8px は箱をやめて浮いた縦から出しているので、総量は増えていない）
- `--sp-4xl`(56px) `--sp-5xl`(80px) は**使わない**
- スクロール総量は現行 +15% 以内。**PR #50 実測：今日 −7.7% / 大きな流れ −4.2% /
  生まれ +3.0% / 暦 +2.0% / 事典 +1.2%**

「毎朝10秒で今日を確認する」体験を壊さないことが、余白の美しさより優先する。

## Motion

**イージングは一本。** `--ease-out: cubic-bezier(.16, 1, .3, 1)`。

- `--spring-out: cubic-bezier(.34, 1.56, .64, 1)` と `--spring-soft` は**廃止**。
  1.56 の跳ね返りは快活であって品格ではない
- duration は 220ms → **420ms**
- `prefers-reduced-motion: reduce` で animation と transition の**両方**を止める

**動くのは空と、金が置かれる過程だけ。**
残す＝`twinkle`（星）`float-y`（月）`retro-drift`（逆行）`draw`（ゲージの描線）

### 触れると静かに応える

「何も起こらない UI」にはしない。ただし**応えかたは紙の語彙で選ぶ** —
浮き上がり（影）と発光は使わず、**縁と罫だけ**が反応する。

- カードの縁が一段はっきりする（`--hairline` → `--hairline-strong`・420ms）
- 兆しカードの色罫が 24px → 44px に伸びる。**動くのは幅ひとつで、組版は 1px も動かない**
- 開運アクションの一字が金へ寄る

`(hover: hover) and (pointer: fine)` で囲う。タッチ端末では hover 状態がタップ後に
貼り付いたまま残る。

**繋ぐ相手は「実際に変わるもの」でなければ意味がない。** ナビの図（`.navbar-ind`）は
`transition: background .2s` を持っていたが、活性は線が担うので background は最初から
動かない ―― つまり**何も繋いでいない宣言**で、実際に変わる色（灰→金）と押下の沈みは
どちらも瞬間で切り替わっていた。線が 460ms かけて席を移る横で図だけが点滅するので、
席の移動と図の点灯が別々の出来事に見えていた。

### 移動には向きがある

5つの席は横に並び、活性の線も横に席を移る。それなのに本文が**下から**立ち上がると、
線と本文が別々の出来事に見えて「どこが変わったのか」が一拍遅れる。
**移った向きへ本文も送る**（`.column[data-dir="next|prev"]`・±26px）と、5タブが
一枚の横長の紙の上に並んでいることになり、隣へ移っただけだと体で分かる。

この向きの一致が、そのまま**横に払ってタブを移す**（タッチ）の下地になる。
下部ナビへ親指を往復させずに隣を見られる。縦の送りを一切奪わないのが条件で、
軸を最初の一動きで決め、`preventDefault` はしない（横のページ送りは
`overflow-x: clip` で元々起きないので、奪うものが無い）。
横へ流れる面（大運・年表）と入力欄から始まった指の動きは、その面自身のものとして渡す。

### 送り — スクロールは操作ではなく素材

Long Document を選んだ時点で、**送ること自体がこのアプリの主要な操作**になった。
それなのに送りの周りだけは既定のまま＝借り物で、三つ安かった。

| | 直しかた |
|---|---|
| タブを移るたび列が 15px 横へ跳ぶ | `scrollbar-gutter: stable`。切替を**縦だけの動き**にする |
| OS 既定の太い灰の帯 | `scrollbar-width: thin` ＋ `--outline` 由来の `scrollbar-color`。空4状態に追随する |
| 横の帯（大運・年表）が端でページごと動く | `overscroll-behavior-x: contain` |

**`scroll-behavior` は CSS に置かない。** 移動は場合で分かれる ——
別のタブへは**跳ぶ**（内容が入れ替わるので、滑らせると消えた本文の上を昇ることになる）、
いま居るタブをもう一度叩いたら**送る**（内容は変わらないので、上へ戻す所作になる）。
CSS で一律にすると前者まで滑る。reduced-motion の分岐も JS 側が持つ
（JS が behavior を明示すると `scroll-behavior: auto !important` は効かない）。

**横の帯の端は切り落とさず溶かす。** どちら側にまだ続きがあるかは
**進度そのもの**が知っているので、`scroll(self inline)` で `--edge-l/-r`（登録済みの
`<length>`）を補間し、マスクの減衰に使う。位置を測る JS は要らない。
帯が溢れていなければ進度域が 0 になってタイムラインが効かず、初期値 0 のまま
＝全面不透明になる（**未対応・非スクロール時に「何も起きない」が正しい退化**、
という `view()` と同じ設計）。

**視差は「送った量」ではなく「ページの進度」に比例させる。** 星図が視野の外へ
持っている逃げは上下 6% しかないので、送った px へ直に掛けると少し下るだけで
逃げを使い切り、**下端に星の無い帯**が残る（実測 127px）。進度 0→1 に逃げを
配分すれば、本文がどれだけ長くても端は空かない。

### 現れかた — スクロール連動

`animation-timeline: view()`。**JS もライブラリも足さない**（依存ゼロの方針は不変）。
未対応のブラウザでは**何も起きない＝内容は最初から見えている**が正しい退化で、
これが「JS が落ちたら白紙」型のリビール実装との決定的な違い。

ふたつ避けること：

1. **二重掛け** — 既に `.rise`（マウント時）を持つ面は `:not(.rise)` で外す
2. **reduced-motion** — 全域のキルスイッチは `animation-duration` を潰すが、
   タイムライン駆動の animation は**進度0の姿＝透明で固まりうる**。
   `@media (prefers-reduced-motion: no-preference)` で丸ごと囲い、**規則そのものを出さない**

入口の見出しは「下から現れる」のではなく **`clip-path` で版から抜き上がる**。
文字が組まれてゆく所作になり、明朝の縦画と合う。終端の inset は負にして、
字送りの余りと縦画の先端を切り落とさない。

## 祝祭 — 「発光」ではなく「箔」

天赦日・一粒万倍日の高揚は**残す**。占術は情報ではなく感情の商品であり、
何も起きない画面はユーザーが開く理由を一つ減らす。変えるのは**語彙だけ**。

| 旧（発光） | 新（箔） |
|---|---|
| 粒が放射状に飛ぶ 1.5s | 空気に乗って**上へ舞う** 2.8s。粒は角丸1pxの箔片。粒数は据え置き（14） |
| `shimmer` 1.2s の速い一閃 | **2.4s** の遅い光の移動 |
| スコアの `text-shadow: 0 0 26px` | **削除**。発光はネオンの語彙 |
| ゲージの `glow-breathe` 光暈 | 振幅を半分に。息づくのは残す |

## 金 — 聖域

金は**塗りと細い縁**で表す。**発光（glow / text-shadow）では表さない。**

`--accent-soft: var(--gold-500)` の定義は維持する（明るい地で gold-300 は
4.2:1 で AA 未達、というコントラスト上の根拠がある）。

## 形

`--shape-full`(999px) の使用は**円**だけに限る（`.lucky-dot` 8px・`.entry-swatch` 14px）。

- カード・ボタン・入力 … `3px`
- チップ・バッジ・タグ … `0`〜`2px`（多くは罫だけ／文字だけ）
- ナビの活性表示 … ピルの塗りをやめ、**細い線が席を移る**

## 流れ線（FlowLine）— このアプリの署名

**細い金線＋ぼんやりした暈（かさ）**の二層。

| 層 | 内容 |
|---|---|
| ① 暈 | 太めの輪郭（`wMax 6.2`）を `feGaussianBlur stdDeviation="8"` で強くぼかす。**常に全長見える**＝流路の予感。濃さは地の明暗で変える（夜 `.40` / 昼 `.18`）— 明るい地で光を広げても見えず濁るだけ |
| ② 金線 | **可変幅の塗り**（ストロークではない）。最大 **2.6px**。スクロールで下へ置かれてゆく |

**可変幅が核心。** 均一 `stroke-width` は「SVG のデフォルト」という最大の機械臭で、
ここを直すだけで質が変わる。幅は書の**入り・抜き**に沿い、同じ mulberry32 シードによる
低周波のゆらぎを乗せて**手で継いだ痕**を出す（決定論的・毎回同一）。

鋭い艶（specular）は**置かない**。2.6px では潰れて安っぽくなる。金属感は
グラデ内の明るい帯（`--gold-100` @ 0.63）だけで出す。

**維持する不変条件**（[docs/SPEC.md](docs/SPEC.md) §7）:
振幅の px クランプ `Math.min(w * (0.13 + 0.17 * amp), 190)` ／
モバイルは `.shell` 基準・デスクトップは `.column` 基準 ／
`useId` による gradient・mask・filter ID の動的採番 ／ reduced-motion で全描画

---

## CTA の声 — 箔押し

ボタンは、和紙と金箔になったこのアプリで**最後まで Material 3 のまま**だった場所だった。
filled / outlined / text / segmented / icon の5型がそろい、hover はどれも
地を 8% 洗うステートレイヤーで、活性は M3 の `--secondary-container`（＝
このアプリのパレットに無い青灰）を1箇所だけ持ち込んでいた。
角丸を 3px にしても、色をトークンに寄せても、**語彙が M3 のままなら M3 に見える**。

置き換えの中心は主CTAで、隠喩は **箔押し**。design.md が既に持っていた語
（「祝祭 — 発光ではなく箔」）を面へ広げただけで、新しい概念は増えていない。

| 段 | 形 | 触れると | 押すと |
|---|---|---|---|
| **主**（`.cta`） | 金箔を紙に**圧した**面。明朝 500・字送り `--tr-wider` | 稜が一段明るくなり、箔の上を反射が**一度だけ**渡る（2.4s・下の「反射は広く弱く」） | 稜が反転する（凹んだ面は光を下から拾う） |
| **副**（`.reset`） | 罫一本・明朝 | 罫と字が金へ寄る | — |
| **開示**（`.disclose`） | 文字＋**罫で引いた**＋／− | ラベルの下に罫が左から引かれる | — |
| **選択**（`.seg-btn`） | 上下の罫だけ。活性は**席を移る線** | 字が金へ寄る | — |

### 箔である条件

平らな塗りは箔ではなく**色紙**になる。三つ要る（硝子の三条件と同じ立て方）。

| | なぜ |
|---|---|
| **① 極浅い縦のグラデ** | 箔は拡散した空の鏡。上が明るい。振り幅は上端でも白文字と **5.1:1**（生成り地）に収まる範囲に限る |
| **② 押した痕は内側の 1px** | 上＝光を拾う稜、下＝圧の影。**外へ落ちる影は使わない**（浮遊は硝子の語彙） |
| **③ 紙の目が乗る** | `--paper-grain` を `soft-light` で（`overlay` だと金の彩度が飛ぶ）。`body::after` の地合いは `fixed` なので面の上には来ない。負の `z-index` に置くと**背景の上・文字の下**に描かれ、文字にはノイズが掛からない |

**稜とグラデの色にはトークンではなく素の alpha を使う。** 光の反射（温白）と光の
欠落（墨）は物理としては地の色に依らないので、4つの空すべてで同じ1本のレシピになる。
実務上の理由も大きい — `color-mix()` は Lightning CSS が非対応ブラウザ用の
**不透明な代替**を必ず前置きするので、箔の身で使うと代替側が `--gold-100` の
べた塗り＝生成り地で白文字が読めない面になる。主CTAで踏む賭けではない。

箔の身は `::before` が持ち、要素の背景は透明にする。`background-image` は補間
されないが `opacity` は補間されるので、**無効→有効を 420ms の実体にできる**。
`.cta:disabled` は「壊れた灰色の箱」ではなく**まだ箔が置かれていない版**（罫だけ）で、
生年月日を入れた瞬間に箔が置かれる。旧実装の 38% の幽霊文字（AA 未達）もここで消える。

### 反射は「広く弱く」

細い帯は縁が立って**スキャン線**に見え、強い帯は箔ではなく**発光**に見える。
どちらも「やりすぎ」の正体は濃さそのものではなく、**帯の形**にある。

| | 帯幅 | 濃さ |
|---|---|---|
| 祝祭（`.hitokoto-shimmer`） | 16% | 0.30 |
| 主CTA（`.cta:hover`） | **40%** | **0.13** |

広く取って薄く落とすと、通ったことは分かるが**何が通ったかは言えない**濃さになる。

**主CTAは祝祭より必ず弱く保つこと。** 主CTAは毎回触れる場所で、祝祭は年に数度の
合図。ここが同じ強さだと、強い側ではなく**祝祭の側が意味を失う**。
稜も同じ理由で上げすぎない（0.45 → 0.58。0.72 は「点いた」に見えた）。

### 禁じたこと（テストで固定）

`__tests__/design-tokens.test.ts` が落とす。散文では守れないので配列と正規表現にした。

- **地を 8% 洗う hover**（M3 のステートレイヤー）— 全域で禁止。応えるのは縁と罫
- **`scale()` の押下** — 伸縮はゴムの語彙。紙は縮まず**沈む**（`translateY(1px)`）
- **反射（`shimmer`）の拡散** — 許可面は `SHIMMER_SURFACES` の2つだけ。
  反射は発光ではないので金の禁則（glow / text-shadow）には触れないが、
  面という面が光り出したら意味を失う

## 実装の作法

- **新規CSSは必ずトークンで書く**。リテラルを使うなら PR 本文に理由を記す
- **意味論的CSS**（`.card` `.flowcard` `.navbar-inner` …）。Tailwind のユーティリティは使わない。
  理由は [docs/SPEC.md](docs/SPEC.md) §7 に詳しい（3軸カスケードは CSS 変数のための設計）
- 占術ロジック（`lib/`）には触らない。見た目の改修が参照値テスト**241件**に波及してはならない
- **意匠の規約はテストで固定する。**「限定的に使う」「増やさない」は散文では守れない。
  硝子面の許可リスト（`GLASS_SURFACES`）のように、**破ると落ちる形**にして
  PR の差分に必ず現れるようにする

## ページ間で必ず共有するもの

ワードマーク／金の置き場所／表示・本文フォント／CTA の声（形・角丸・余白の律動）／
章題の律動（節気色の縦罫＋明朝の見出し＋溶ける罫）／イージング／
**眉ラベル＋大きな明朝の値**という値の組み方（ダッシュボードの「生まれのしるし」と
入口の「今日の空」は同じ形。片方を直したらもう片方も見る）

## フォームは別の言語で喋らない

入口のフォームは**このアプリで唯一の入力面**で、放っておくと汎用の SaaS フォームに
戻る。ラベルは金の帯ではなく**眉**（`--fs-caption` ＋ `--tr-wider`）、
ヒントは本文と同じ声で書く。

**任意項目は「任意・より詳しく」ではなく、入れると何が増えるかを書く。**
出生時刻は「四柱が4本そろいます」。曖昧な期待をさせるより、
**実測に裏打ちされた具体**の方が入力してもらえる（数値は
[docs/SPEC.md](docs/SPEC.md) §8）。書けない＝そもそも価値が薄い、という検算にもなる。

## ページごとに変えてよいもの

Long Document の中での構成順、眉ラベルの語、記号の選択。
**テーマ・色・書体・イージングは変えない。**
